rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes - Arvaan Collective marketplace
    // Products, sellers, orders are managed by authenticated sellers
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
